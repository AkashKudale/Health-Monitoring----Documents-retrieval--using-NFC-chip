<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owner.nfcadminapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AdminActivity"
            android:label="@string/title_activity_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddUser" />
        <activity android:name=".WriteNFC" />
        <activity android:name=".AddDoctor" />
        <activity android:name=".EditRecord" />
        <activity android:name=".FinalEditRecord" />
        <activity android:name=".AboutUs" />
        <activity android:name=".MyProfile" />
        <activity android:name=".AddPolice"
            android:label="Add Police"/>
        <activity android:name=".AddRto"
            android:label="Add RTO"></activity>
    </application>

</manifest>